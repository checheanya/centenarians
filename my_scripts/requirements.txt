# Proteomics notebook + my_scripts/utils
#
#   pip install -r requirements.txt
#
# Optional: use a dedicated venv, e.g.  python -m venv .venv && source .venv/bin/activate

# --- Core data / numerics ---
numpy>=1.20
pandas>=1.3
scipy>=1.7

# --- Plotting ---
matplotlib>=3.5
seaborn>=0.12
adjustText>=0.8

# Escape scatter (ggplot2 + ggrepel via R); install R and: install.packages(c("ggplot2","ggrepel"))
rpy2>=3.5

# --- ML / stats (PCA, imputation, multiple testing) ---
scikit-learn>=1.0
statsmodels>=0.13

# --- Notebook: Excel metadata (pd.read_excel on .xlsx) ---
openpyxl>=3.0

# --- Pathway enrichment (Enrichr via gseapy; needs network) ---
gseapy>=1.0
requests>=2.26
